---
title: Configuration
weight: 40
description: Every command, flag, config key, file and path, one line each.
---

# Configuration

One page, for looking things up: what each command takes, what the config file accepts, and where every file lives. The explanations are elsewhere; this is the list.

- **[Reference](reference.md)**: every subcommand and flag with its default, the config file format, the files and paths, the pool markers, and the exit status.
